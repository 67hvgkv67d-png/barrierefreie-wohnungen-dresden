name: Wohnungsangebote aktualisieren

on:
  workflow_dispatch:
  schedule:
    - cron: "0 6 * * 1,3"

permissions:
  contents: write

concurrency:
  group: wohnungsangebote-import
  cancel-in-progress: false

jobs:
  import:
    runs-on: ubuntu-latest
    steps:
      - name: Repository auschecken
        uses: actions/checkout@v4

      - name: Node.js einrichten
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Angebote aus allen Quellen abrufen
        run: npm run import:all

      - name: Änderungen veröffentlichen
        run: |
          if git diff --quiet -- data/wohnungen.json; then
            echo "Keine Änderungen an den Wohnungsdaten."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/wohnungen.json
          git commit -m "Aktualisiere Wohnungsangebote"
          git push
